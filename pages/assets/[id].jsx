import { useEffect, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/router";
import { collection, doc, getDoc, getDocs, query, where, orderBy } from "firebase/firestore";
import { db } from "../../lib/firebase";
import ProtectedRoute from "../../components/ProtectedRoute";
import Layout from "../../components/Layout";
const badgeClass=(s)=>s==="صالح"?"bg-green-50 text-green-700":s==="عاطل"?"bg-amber-50 text-amber-700":"bg-red-50 text-red-700";
export default function AssetDetails(){const router=useRouter(); const {id}=router.query; const [asset,setAsset]=useState(null); const [moves,setMoves]=useState([]); const [preview,setPreview]=useState(false); useEffect(()=>{if(!id)return; getDoc(doc(db,"assets",id)).then(s=>{if(s.exists())setAsset({id:s.id,...s.data()})}); getDocs(query(collection(db,"assetMovements"),where("assetId","==",id),orderBy("movedAt","desc"))).then(s=>setMoves(s.docs.map(d=>({id:d.id,...d.data()})))).catch(()=>getDocs(query(collection(db,"assetMovements"),where("assetId","==",id))).then(s=>setMoves(s.docs.map(d=>({id:d.id,...d.data()})))));},[id]); if(!asset)return <ProtectedRoute><Layout title="تفاصيل المعدة"><div className="page-card p-5">جاري التحميل...</div></Layout></ProtectedRoute>;
 return <ProtectedRoute><Layout title={`تفاصيل المعدة - ${asset.name}`}><div className="grid gap-4 xl:grid-cols-3"><div className="page-card p-5 xl:col-span-2"><div className="flex flex-col gap-4 md:flex-row"><button onClick={()=>asset.imageUrl&&setPreview(true)} className="h-64 w-full overflow-hidden rounded-3xl bg-slate-100 md:w-80">{asset.imageUrl?<img src={asset.imageUrl} className="h-full w-full object-cover" alt={asset.name}/>:null}</button><div className="flex-1 space-y-3"><h1 className="text-2xl font-black">{asset.name}</h1><div className="flex flex-wrap gap-2"><span className={`badge ${badgeClass(asset.status)}`}>{asset.status}</span><Link className="badge bg-slate-100 text-slate-700" href={`/assets?assetTypeId=${asset.assetTypeId||""}`}>{asset.assetTypeName||"مكينة"}</Link><Link className="badge bg-blue-50 text-blue-700" href={asset.placeType==="kubra"?`/assets?kubraId=${asset.kubraId||asset.placeId}`:`/assets?farmId=${asset.farmId||asset.placeId}`}>{asset.placeName||asset.farmName||asset.kubraName||"-"}</Link></div><p className="text-sm text-slate-500">الكود: {asset.code||"-"}</p><p className="text-sm text-slate-500">العمال: {asset.workerNames||"-"}</p><p className="rounded-2xl bg-slate-50 p-4 text-sm leading-7 text-slate-600">{asset.notes||"لا توجد ملاحظات"}</p><div className="flex flex-wrap gap-2"><Link className="btn-primary" href={`/assets/edit/${asset.id}`}>تعديل</Link><Link className="btn-secondary" href={`/assets/move/${asset.id}`}>نقل المعدة</Link></div></div></div></div><div className="page-card p-5"><h3 className="mb-4 font-black">روابط سريعة</h3><div className="space-y-2"><Link className="btn-secondary w-full" href={`/assets?status=${asset.status}`}>معدات بنفس الحالة</Link>{asset.assetTypeId&&<Link className="btn-secondary w-full" href={`/assets?assetTypeId=${asset.assetTypeId}`}>معدات بنفس النوع</Link>}{(asset.workerIds||[]).map(w=><Link key={w} className="btn-secondary w-full" href={`/assets?workerId=${w}`}>معدات عامل مرتبط</Link>)}</div></div></div><div className="page-card mt-4 overflow-x-auto"><h3 className="p-5 pb-2 font-black">سجل حركة المعدة</h3><table className="w-full"><thead className="bg-slate-50"><tr><th className="table-th">من</th><th className="table-th">إلى</th><th className="table-th">السبب</th><th className="table-th">التاريخ</th></tr></thead><tbody>{moves.map(m=><tr key={m.id} className="border-t border-slate-100"><td className="table-td">{m.fromPlaceName||"-"}</td><td className="table-td">{m.toPlaceName||"-"}</td><td className="table-td">{m.reason||"-"}</td><td className="table-td">{m.movedAt?.toDate?m.movedAt.toDate().toLocaleString("ar-EG"):"-"}</td></tr>)}</tbody></table></div>{preview&&<div onClick={()=>setPreview(false)} className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4"><div className="max-h-[90vh] max-w-5xl rounded-3xl bg-white p-4"><button className="btn-secondary mb-3" onClick={()=>setPreview(false)}>إغلاق</button><img src={asset.imageUrl} className="max-h-[75vh] w-full object-contain" alt={asset.name}/></div></div>}</Layout></ProtectedRoute>}
