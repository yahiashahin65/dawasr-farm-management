import { useEffect, useState } from "react";
import { useRouter } from "next/router";
import { addDoc, collection, doc, getDoc, getDocs, serverTimestamp, updateDoc } from "firebase/firestore";
import { db } from "../../../lib/firebase";
import ProtectedRoute from "../../../components/ProtectedRoute";
import Layout from "../../../components/Layout";
export default function MoveAsset(){const router=useRouter(); const {id}=router.query; const [asset,setAsset]=useState(null); const [farms,setFarms]=useState([]); const [kubras,setKubras]=useState([]); const [form,setForm]=useState({placeType:"farm",placeId:"",reason:""}); const [loading,setLoading]=useState(false); useEffect(()=>{getDocs(collection(db,"farms")).then(s=>setFarms(s.docs.map(d=>({id:d.id,...d.data()})))); getDocs(collection(db,"kubras")).then(s=>setKubras(s.docs.map(d=>({id:d.id,...d.data()}))));},[]); useEffect(()=>{if(id)getDoc(doc(db,"assets",id)).then(s=>{if(s.exists()){const d={id:s.id,...s.data()}; setAsset(d); setForm(p=>({...p,placeType:d.placeType||"farm",placeId:d.placeId||d.farmId||d.kubraId||""}))}})},[id]); const places=form.placeType==="farm"?farms:kubras; const submit=async(e)=>{e.preventDefault(); if(!form.placeId)return alert("اختر المكان الجديد"); const place=places.find(x=>x.id===form.placeId); setLoading(true); try{await updateDoc(doc(db,"assets",id),{placeType:form.placeType,placeId:form.placeId,placeName:place?.name||"",currentPlace:{type:form.placeType,id:form.placeId,name:place?.name||""},farmId:form.placeType==="farm"?form.placeId:"",farmName:form.placeType==="farm"?place?.name||"":"",kubraId:form.placeType==="kubra"?form.placeId:"",kubraName:form.placeType==="kubra"?place?.name||"":"",updatedAt:serverTimestamp()}); await addDoc(collection(db,"assetMovements"),{assetId:id,assetName:asset?.name||"",fromPlaceType:asset?.placeType||"",fromPlaceId:asset?.placeId||"",fromPlaceName:asset?.placeName||asset?.farmName||asset?.kubraName||"",toPlaceType:form.placeType,toPlaceId:form.placeId,toPlaceName:place?.name||"",reason:form.reason||"نقل المعدة",movedAt:serverTimestamp(),createdAt:serverTimestamp()}); router.push(`/assets/${id}`);}catch(e){console.error(e); alert("حدث خطأ أثناء النقل"); setLoading(false);}}; return <ProtectedRoute><Layout title="نقل المعدة"><form onSubmit={submit} className="page-card max-w-3xl space-y-4 p-5"><div className="rounded-2xl bg-slate-50 p-4 text-sm text-slate-600"><b>{asset?.name}</b><br/>المكان الحالي: {asset?.placeName||asset?.farmName||asset?.kubraName||"-"}</div><div className="grid gap-4 md:grid-cols-2"><select className="form-input" value={form.placeType} onChange={e=>setForm({...form,placeType:e.target.value,placeId:""})}><option value="farm">نقل إلى مزرعة</option><option value="kubra">نقل إلى الكِبرة</option></select><select className="form-input" value={form.placeId} onChange={e=>setForm({...form,placeId:e.target.value})}><option value="">اختر المكان الجديد</option>{places.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></div><textarea className="form-input h-28" placeholder="سبب النقل أو ملاحظات الحركة" value={form.reason} onChange={e=>setForm({...form,reason:e.target.value})}/><button disabled={loading} className="btn-primary">{loading?"جاري النقل...":"حفظ حركة النقل"}</button></form></Layout></ProtectedRoute>}
